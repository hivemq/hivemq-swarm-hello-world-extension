[versions]
hivemq-platform = "4.20.0"
jetbrains-annotations = "26.1.0"

[libraries]
hivemq-swarm-extensionSdk = { module = "com.hivemq:hivemq-swarm-extension-sdk", version.ref = "hivemq-platform" }
jetbrains-annotations = { module = "org.jetbrains:annotations", version.ref = "jetbrains-annotations" }

[plugins]
defaults = { id = "io.github.sgtsilvio.gradle.defaults", version = "0.3.0" }
spotless = { id = "com.diffplug.spotless", version = "8.9.0" }
